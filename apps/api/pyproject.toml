[project]
name = "caudal-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "psycopg[binary]",
    "alembic",
]

[tool.uv]
package = false

[tool.pyright]
typeCheckingMode = "strict"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: caudal
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: caudal
    ports:
      - "5432:5432"
    volumes:
      - caudal_pgdata:/var/lib/postgresql/data

volumes:
  caudal_pgdata:

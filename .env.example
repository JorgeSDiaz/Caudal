# Template for local secrets. Copy to .env and fill in real values:
#   cp .env.example .env
# For production, do NOT use a .env file — use Docker secrets or a cloud secret manager.
POSTGRES_USER=caudal
POSTGRES_PASSWORD=change-me
POSTGRES_DB=caudal
